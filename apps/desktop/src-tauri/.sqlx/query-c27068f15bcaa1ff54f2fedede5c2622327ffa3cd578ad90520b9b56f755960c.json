{
  "db_name": "SQLite",
  "query": "\n            WITH RECURSIVE dates(date) AS (\n                VALUES(date('now', '-6 days'))\n                UNION ALL\n                SELECT date(date, '+1 day')\n                FROM dates\n                WHERE date < date('now')\n            )\n            SELECT \n                dates.date as \"date!: String\",\n                (COALESCE(SUM(s.total), 0.0) + COALESCE(SUM(so.total), 0.0)) as \"amount!: f64\"\n            FROM dates\n            LEFT JOIN sales s ON date(s.created_at) = dates.date AND s.status != 'CANCELED'\n            LEFT JOIN service_orders so ON date(so.completed_at) = dates.date AND so.status = 'DELIVERED'\n            GROUP BY dates.date\n            ORDER BY dates.date\n            ",
  "describe": {
    "columns": [
      {
        "name": "date!: String",
        "ordinal": 0,
        "type_info": "Text"
      },
      {
        "name": "amount!: f64",
        "ordinal": 1,
        "type_info": "Float"
      }
    ],
    "parameters": {
      "Right": 0
    },
    "nullable": [
      true,
      false
    ]
  },
  "hash": "c27068f15bcaa1ff54f2fedede5c2622327ffa3cd578ad90520b9b56f755960c"
}
