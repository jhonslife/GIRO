name: Nightly Build

on:
  schedule:
    - cron: "0 3 * * *" # 3 AM UTC diariamente
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  nightly:
    name: Nightly Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"
          cache-dependency-path: pnpm-lock.yaml

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev build-essential curl wget file libssl-dev libayatana-appindicator3-dev librsvg2-dev libudev-dev

      - name: Install Node dependencies
        run: pnpm install --frozen-lockfile

      - name: Run full test suite
        run: |
          pnpm --filter @giro/desktop run test:run || true
          pnpm --filter @giro/desktop run typecheck || true

      - name: Run Rust tests
        working-directory: apps/desktop/src-tauri
        run: cargo test

      - name: Build check
        working-directory: apps/desktop/src-tauri
        run: cargo build --release

      - name: Report status
        if: failure()
        run: |
          echo "::error::Nightly build failed!"
